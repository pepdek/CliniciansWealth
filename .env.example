# Clinician Loan Optimizer - Environment Variables

# OpenAI Configuration (Required for document analysis)
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# Stripe Configuration (Required for payments)
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key-here
STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key-here
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret-here

# Client Environment Variables (.env file in /client directory)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key-here

# Email Configuration (Optional - will use console logging if not configured)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com  
SMTP_PASS=your-app-specific-password

# Application Configuration
NODE_ENV=development
PORT=4545

# Database Configuration (Optional - currently using in-memory storage)
# DATABASE_URL=postgresql://username:password@localhost:5432/clinician_loans

# Security
SESSION_SECRET=your-super-secret-session-key-here

# Analytics/Monitoring (Optional)
# MIXPANEL_TOKEN=your-mixpanel-token
# SENTRY_DSN=your-sentry-dsn

# Deployment Notes:
# 1. For production, use live Stripe keys (sk_live_... and pk_live_...)
# 2. Set NODE_ENV=production
# 3. Configure proper SMTP credentials for email delivery
# 4. Set up Stripe webhooks pointing to your domain/api/mcp/payment/webhook
# 5. Ensure OPENAI_API_KEY has sufficient credits for document analysis